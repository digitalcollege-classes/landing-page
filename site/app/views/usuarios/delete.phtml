<h2>Excluir Usuario</h2>
<hr>

<div class="alert alert-warning">
    <strong>Atencion!</strong> Tem certeza que deseja excluir o usuario <strong><?= htmlspecialchars(
        $usuario["nome"],
    ) ?></strong>?
</div>

<div class="card">
    <div class="card-body">
        <p><strong>Nome:</strong> <?= htmlspecialchars($usuario["nome"]) ?></p>
        <p><strong>Email:</strong> <?= htmlspecialchars(
            $usuario["email"],
        ) ?></p>
    </div>
</div>

<form action="/admin/usuarios/excluir/<?= htmlspecialchars(
    (string) $usuario["id"],
) ?>" method="post" class="mt-3">
    <button type="submit" class="btn btn-danger">Confirmar Exclusao</button>
    <a href="/admin/usuarios/listar" class="btn btn-secondary">Cancelar</a>
</form>
