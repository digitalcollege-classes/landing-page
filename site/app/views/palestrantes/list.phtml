<div class="d-flex justify-content-between align-items-center">
    <h2>Listar Palestrantes</h2>
    <a href="/admin/palestrantes/cadastrar" class="btn btn-primary">Novo Palestrante</a>
</div>
<hr>

<?php if (empty($palestrantes)): ?>
    <div class="alert alert-info">Nenhum palestrante cadastrado.</div>
<?php else: ?>
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Email</th>
                <th>Tema</th>
                <th>Criado em</th>
                <th>Acoes</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($palestrantes as $palestrante): ?>
                <tr>
                    <td><?= htmlspecialchars(
                        (string) $palestrante["id"],
                    ) ?></td>
                    <td><?= htmlspecialchars($palestrante["nome"]) ?></td>
                    <td><?= htmlspecialchars($palestrante["email"]) ?></td>
                    <td><?= htmlspecialchars($palestrante["tema"]) ?></td>
                    <td><?= htmlspecialchars($palestrante["criado_em"]) ?></td>
                    <td>
                        <a href="/admin/palestrantes/editar/<?= $palestrante[
                            "id"
                        ] ?>" class="btn btn-sm btn-warning">Editar</a>
                        <a href="/admin/palestrantes/excluir/<?= $palestrante[
                            "id"
                        ] ?>" class="btn btn-sm btn-danger">Excluir</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php endif; ?>
